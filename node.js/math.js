const PI = 3.14;
module.exports.PI = PI;
const MAX = (a, b) => {
    if (a > b) {
        return a;
    } else {
        return b;
    }
}
module.exports.MAX = MAX;
const MIN = (a, b) => {
    if (a > b) {
        return b;
    } else {
        return a;
    }
}
module.exports.MIN = MIN;

const SQRT = (a) => {
    let asdfghjkl = Boolean;
    for (let i = 0; asdfghjkl = true; i++) {
        if (i * i == a) {
            return i;
        } else if (i * i > a) {
            return (undefined)
        }
    }
}
module.exports.SQRT = SQRT;
const ROUND = (a, b) => {
    // a = a.split();
    console.log["aasas"];
    ans = [];
    for (let i = 0; i + 1 <= b; i++) {
        ans.push(a[i]);
    }
    let str = String(a)
    for (let z = 0; z + 1 <= str.length - b; z++) {
        ans.push(0);
    }

    return (ans);
}
module.exports.ROUND = ROUND;